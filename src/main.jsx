import React from 'react';
import ReactDOM from 'react-dom/client';
import { FirstApp, SecodnApp } from './FirstApp';


ReactDOM.createRoot( document.getElementById('root') ).render(
    <React.StrictMode>
        <SecodnApp />
    </React.StrictMode>
)
